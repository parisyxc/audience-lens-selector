# Audience Lens Selector — Tool Spec

> 把 `consumer-persona-evaluation`（人群视角判断）框成一个 **input → output 的工具**。
> 引擎不变：`SKILL.md`（6 步工作流）+ `references/`（视角库 · 选择规则 · 输出契约 · 出处说明）。改的是外壳、命名、I/O 契约。
> 定位：tool-led · **「帮你想」** —— 在做任何人群研究**之前**，先判断「该从哪个切口看人」。

---

## 1 · 是什么（一句话）

**Audience Lens Selector** —— 输入一份营销 brief，输出一份**人群研究视角 brief**：这次该用哪一条主轴去切人群（需求/痛点、决策心态、场合、品类专业度、采用阶段……），为什么是它、为什么不是那个更顺手的切法，以及交给下游 social listening / 访谈 / 问卷时具体去对比什么。

它回答的不是「目标人群是谁」，而是 **「研究人群之前，该怎么看人」**。

## 2 · Input → Output 契约

**INPUT（3 项，第 2 项最关键）**

| # | 输入 | 含义 | 缺失时 |
|---|---|---|---|
| 1 | **Brand / offer** | 品类、产品、价格带、渠道 / 通路 | 必问 |
| 2 | **Marketing decision** | 这次人群研究要支撑**哪个决策**，希望人做出什么行为改变 | **Blocker** —— 工具的第一原则是「从决策出发，不从惯用的分群模型出发」 |
| 3 | **Current audience assumptions**（可选） | 你现在以为的目标人群是谁，证据是什么 | 可缺；缺时列为 `[U]` 未知项 |

可选补充：地域 / 资格限制、现有用户数据、时间与预算、渠道约束。
信息不全时**带着显式假设继续**，只追问「答案会改变主轴选择」的问题（≤ 5 个）。

**OUTPUT —— 一份 Audience Research Lens Brief（6 部分，规范见 `references/output-contract.md`）**

| # | 部分 | 内容 |
|---|---|---|
| 1 | **决策框定** | 一句话：「我们需要理解 ___ 上的差异，好让团队决定 ___，而不预设 ___。」 |
| 2 | **推荐的看人方式** | 五角色表：主轴 · 副轴（0–1 个）· 资格过滤 · 触达标签 · 画像描述；+ 为什么选它 + **被否决的诱人切法** + 置信度 |
| 3 | **临时对照卡** | 2–4 组，全部标 `[H]`：定义 · 纳入信号 · 对照信号 · 后续要找的证据 · 会改变的营销决策 · **证伪条件** |
| 4 | **研究维度优先级** | P0 决定性轴 / P1 边界轴 / P2 描述变量 —— 不是泛泛的 checklist |
| 5 | **下游研究交接** | 主研究问题 · 要对比的人群或语料 · 要找的行为 / 语言 / 证据 / 反信号 · 混淆因素 |
| 6 | **证据台账** | `[F]` 事实 · `[C]` 客户说法 · `[H]` 本次引入的假设 · `[U]` 未知；以 stop condition 收尾 |

> 一句话：**给一份 brief，拿回一个「该怎么切人群」的研究角度 —— 以及它会怎样被证伪。**

## 3 · 核心引擎（它凭什么可靠）

**① 四类人群变量分离 —— 这个工具最锋利的一刀。** 同一个词，可能是四种完全不同的东西：

| 类型 | 回答的问题 | 例子 |
|---|---|---|
| **策略主轴** | 什么差异解释了**不同的反应**？ | 先试再说 vs 要证据才动 |
| 资格过滤 | 谁必须在范围内？ | 在首发市场；问题反复出现；掌握预算 |
| 触达标签 | 之后去**哪里找到**他们？ | 游戏 / 开发者 / 某兴趣社区 |
| 画像描述 | 用什么来描述、检查代表性？ | 年龄、城市级别、家庭结构、职业 |

「找得到」不等于「为什么行动」。最常见的错误，是把一个方便投放的兴趣标签当成人群策略。

**② 12 条视角库**（`lens-library.md`）—— 需求 / 痛点 / 任务 · 决策心态 · 行为 / 场合 · 品类专业度 · 采用阶段 · 与解决方案的关系 / 转换阶段 · 人生转折 · 决策角色 · 价值 / 身份 / 文化取向 · 反漏斗距离 · 价值 / 经济性 · 关系 / 忠诚度。是菜单，不是清单。

**③ 六项 0–2 打分**（`selection-rules.md`）—— 决策相关性 · 解释区分度 · 可观察性 · 可行动性 · 非冗余 · 证据可信度。**分数用来暴露判断，不用来模拟确定性**；决策相关性或可观察性得 0 的视角，不能当主轴。

**④ 8 种业务模式的起手式** —— 护肤 / 症状型 → 痛点；新创意 / AI 工具 → 心态或采用阶段；成熟快消 → 场合；时尚 / 身份品类 → 文化取向；B2B → 决策角色；订阅留存 → 关系阶段；高端化 → 价值诉求；破圈扩张 → 反漏斗距离。只是起点，brief 可以推翻它。

**⑤ 状态 vs 场景** —— 同一场景里的人需要不同的安心 / 证据 → 选**状态**；时间、地点、同伴、约束改变了需求本身 → 选**场景**。两者**各自独立**改变决策时才叠加。

**⑥ 最少推荐规则** —— 1 个主轴、≤ 1 个副轴、2–4 个对照。判断不了时，给出两条最强的竞争视角 + **一个能决出胜负的问题**。

## 4 · 纪律必须保留（这是它的价值，不能稀释）

工具**在推荐研究角度之后就停下**。明确不做：

- 不爬数据、不做 social listening、不上网搜索
- 不声称任何洞察已被验证
- **不写虚构 persona** —— 没有名字、传记、引语、同理心地图
- 不估算人群规模或占比，不选定最终商业目标人群
- 不因为某平台有这个标签，就推荐按它投放

规则里写明了九种要主动规避的失败模式：人口统计默认 · 兴趣分类学 · **persona 表演** · 矩阵爆炸 · 固定类型谬误 · 场景炫目 · 创新者表演 · 过早定目标 · 平台标签绝对化。

每份产出以这句收尾：

> Stop condition: the research angle is specified; no audience insight, final persona, segment size or targeting decision has been claimed.

→ 和 Cultural Brand Analyzer 的「Deliberately not」同理，**这份克制就是差异化**。很多「AI 用户画像」工具产出的，恰恰是它拒绝产出的东西：一个听起来很真、却没人验证过的虚构人物。

## 5 · 和另外三个工具的关系

| | GEO Content Planner | Cultural Brand Analyzer | Product Video Studio | **Audience Lens Selector** |
|---|---|---|---|---|
| 帮你 | 做（内容计划） | 想（文化意义） | 做（创意生产） | **想（研究前的切口）** |
| 输出 | Query Map / AU / 分发 | 6 段文化分析 | 场景 → 冲突 → 脚本 → 分镜 | **人群研究视角 brief** |
| 链路位置 | 执行 + 月度追踪 | 品牌立场 | 创意物料 | **研究的上游 · Step 0** |
| 节奏 | 月度 | 一次性 | 批量 | 每个 brief 一次 |

**这是第一个明确位于另一个工具上游的工具。** 第 5 部分「下游研究交接」可以直接喂给：

- **GEO Content Planner 的 Step 1 · Query Finding**（Reddit / 社区 listening）—— 先定切口再去听，listening 就不会变成漫无目的的收集；
- 工具箱里的 **2.2 数字民族志**，以及已封装的 `run-digital-ethnography` skill。

它同时**来自框架库**：方法论源头之一是 `商业创意策划工具箱/2.4-Persona`。所以它正好是「帮你想（免费框架）→ 帮你做（工具）」两层交叉链接的第一个现成例子 —— 2.4 Persona 的框架页底部，应该指向这个工具。

## 6 · 命名与隐私

- **建议公开工具名：`Audience Lens Selector`**（中文：人群视角判断）。
  原 id `consumer-persona-evaluation` 和工具的核心边界**是矛盾的** —— 它明确拒绝产出 persona，却以 persona 命名。按成果命名，它选的是「研究视角」，产出物本身就叫 *Audience Research Lens Brief*。
  若改名：repo 名、README、网站卡片用新名；`SKILL.md` 的 `name:` 与 `agents/openai.yaml` 里的 `$consumer-persona-evaluation` 引用要**一起改或一起保留**，不能只改一处。
- **引擎**：`consumer-persona-evaluation/`（`SKILL.md` + `references/` 4 件 + `agents/openai.yaml`）—— 自包含，无运行时外部依赖（`source-map.md` 里提到的工具箱文件只是出处说明，不是运行依赖）。附带的 `openai.yaml` 意味着它也能在 OpenAI / Codex 类 agent 中调用。
- **隐私检查**：自动扫描客户名 = 0；但**人工通读发现 `references/source-map.md` 点名了一家真实公司作为示例来源**（名字不写在本文件里，因为本文件会随仓库公开），另外提到一个私有参考文件夹，以及一份社媒平台发布的人群分类报告。公开版处理：
  - **已确认属于客户工作** → 公开版 `source-map.md` 改写为「an AI 3D-creation tool brief」，保留方法论贡献，去掉名字（内部 workspace 版本保留原名）；
  - 私有文件夹名 → 改为「用户提供的人群判断参考资料」；
  - 平台报告 → 保留为出处，但不复述内容（source-map 本身已写明它的时效与平台局限）；
  - `SKILL.md` 正文里的例子（游戏 / 3D 打印 / 开发者 / AI 爱好者 → 先试派 vs 要证据派）不含品牌名，可保留。
- **公开 demo**：`audience-lens-selector-demo.html` ✅ —— 虚构品牌 **Relay**，accent 色 **rose `#F48FB8`**（色相 336°，与 teal / periwinkle / amber 均相距 ≥ 60°）。
  主体：一个虚构的**二手高端公路车交易平台** —— 「骑行者 / Strava 用户 / 铁三」是触达标签；真正的主轴可能是「品类专业度 × 证据门槛」（懂参数的老手 vs 怕买到事故车的升级新手）。避开 AI / 3D 工具品类，免得和示例来源撞。

  ## 7 · 下一步（intake 清单）

- [x] 公开名：`Audience Lens Selector`（demo 已按此命名）
- [x] 示例公司 = 客户工作 → 公开版 source-map 泛化为「an AI 3D-creation tool brief」
- [x] `assets/lens-brief-template.md`：由 `output-contract.md` 派生的可填模板 + 交付前检查清单
- [x] demo HTML：`audience-lens-selector-demo.html`（rose `#F48FB8`）
- [x] 双语 README → 隐私清洗（公开版 source-map 已泛化）→ repo 打包：`outputs/github-ready/audience-lens-selector.zip`
- [x] 已发布：`github.com/parisyxc/audience-lens-selector`（11 个文件逐个校验通过，根目录无重复）
- [x] 网站第 4 张卡片（`t4_*` 20 个 key，EN / 中文 完全对齐；rose accent；首页文案 three → four）
- [ ] 工具箱 2.4 Persona 框架页加一条指向本工具的链接 —— 等免费框架库上站时一起做

